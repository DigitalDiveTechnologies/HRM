'use client';

import { useEffect, useState } from 'react';
import PortalShell from '@/components/PortalShell';
import { api, session, value } from '@/lib/api';
export default function Profile() { const [data, setData] = useState(null); const [team, setTeam] = useState(null); const [error, setError] = useState(''); useEffect(() => { const id = session.get()?.user?.employeeId; if (!id) return; Promise.all([api('/ess/' + id), api('/leave/team/summary')]).then(([ess, summary]) => { setData(ess); setTeam(summary); }).catch((e) => setError(e.message)); }, []); const profile = data?.profile || {}; const docs = data?.documents || []; const company = value(profile, 'divisionName', 'division_name', 'companyName', 'company_name'); return <PortalShell title="My Profile" subtitle="Your employment information and documents"><>{error && <div className="error">{error}</div>}{!data ? <div className="loading">Loading profile…</div> : <><section className="profile-card"><div className="profile-avatar">{String(value(profile, 'fullName', 'full_name') || 'E').slice(0, 1)}</div><div><h2>{value(profile, 'fullName', 'full_name')}</h2><p>{value(profile, 'jobTitle', 'job_title') || 'Employee'}</p>{team?.isTeamLead && <span className="status">Team lead</span>}</div></section><section className="panel"><h2>Employment Details</h2><div className="detail-grid"><Detail label="Employee code" value={value(profile, 'empCode', 'emp_code')} /><Detail label="Email" value={value(profile, 'email')} /><Detail label="Department" value={value(profile, 'departmentName', 'department_name')} /><Detail label="Job title" value={value(profile, 'jobTitle', 'job_title')} /><Detail label="Phone" value={value(profile, 'phone')} />{company ? <Detail label="Company" value={company} /> : null}<Detail label="Join date" value={value(profile, 'joinDate', 'join_date')} /></div></section><section className="panel"><h2>My Documents</h2><div className="table-wrap"><table><thead><tr><th>Document</th><th>Type</th><th>Issue date</th><th>Expiry date</th><th>Status</th></tr></thead><tbody>{docs.map((d) => <tr key={value(d, 'id')}><td>{value(d, 'title')}</td><td>{value(d, 'docType', 'doc_type')}</td><td>{value(d, 'issueDate', 'issue_date') || '—'}</td><td>{value(d, 'expiryDate', 'expiry_date') || '—'}</td><td><span className="status">{value(d, 'status')}</span></td></tr>)}{!docs.length && <tr><td colSpan="5">No documents found.</td></tr>}</tbody></table></div></section></>}</></PortalShell>; }
function Detail({ label, value: content }) { return <div><small>{label}</small><strong>{content || '—'}</strong></div>; }

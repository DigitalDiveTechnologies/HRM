'use client';

import { useEffect, useState } from 'react';
import PortalShell from '@/components/PortalShell';
import { api, value } from '@/lib/api';
export default function Leaves() { const [leaves, setLeaves] = useState([]); const [balances, setBalances] = useState([]); const [error, setError] = useState(''); useEffect(() => { Promise.all([api('/leave'), api('/leave/balances')]).then(([l, b]) => { setLeaves(l); setBalances(b); }).catch((e) => setError(e.message)); }, []); return <PortalShell title="Leaves" subtitle="Your leave balance and request history"><>{error && <div className="error">{error}</div>}<section className="balance-grid">{balances.map((r) => <div className="balance" key={value(r, 'leaveType', 'leave_type')}><small>{value(r, 'leaveType', 'leave_type')}</small><strong>{value(r, 'remainingDays', 'remaining_days')} days</strong><span>{value(r, 'usedDays', 'used_days')} used of {value(r, 'entitlementDays', 'entitlement_days')}</span></div>)}</section><section className="panel"><h2>Leave History</h2><div className="table-wrap"><table><thead><tr><th>Leave type</th><th>From</th><th>To</th><th>Days</th><th>Reason</th><th>Status</th></tr></thead><tbody>{leaves.map((r) => <tr key={value(r, 'id')}><td>{value(r, 'leaveType', 'leave_type')}</td><td>{value(r, 'startDate', 'start_date')}</td><td>{value(r, 'endDate', 'end_date')}</td><td>{value(r, 'days')}</td><td>{value(r, 'reason') || '—'}</td><td><span className="status">{value(r, 'workflowStage', 'workflow_stage', 'status')}</span></td></tr>)}{!leaves.length && <tr><td colSpan="6">No leave requests found.</td></tr>}</tbody></table></div></section></></PortalShell>; }
